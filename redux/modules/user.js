// Import 

// Actions

// Action Creators

// API Actions

// Initial State

const intialState = {
  isLoggedIn: false
}

// Reducer

function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

// Reducer Functions

// Exports

// Default Reducer Export

export default reducer;